name: Tests

on: [push, workflow_dispatch, pull_request]

jobs:
  # Run unittest tests.
  unittest:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run: dotnet test
      